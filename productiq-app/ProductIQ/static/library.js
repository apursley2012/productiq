
document.addEventListener('DOMContentLoaded',()=>{ const { $,esc,money,load,price,title,img,category,subcategory}=PIQ; let results=load();
 const cats=[...new Set(results.map(category))].sort(); $('#lib-category').innerHTML='<option value="">All categories</option>'+cats.map(x=>`<option>${esc(x)}</option>`).join('');
 function render(){let q=$('#lib-search').value.toLowerCase(),c=$('#lib-category').value,s=$('#lib-sort').value;let rows=results.filter(r=>!c||category(r)===c).filter(r=>[title(r),r.asin,r.brand,r.sourceInput?.sku,category(r),subcategory(r)].filter(Boolean).join(' ').toLowerCase().includes(q));
 rows.sort((a,b)=>s==='margin'?(b.pricing?.estimatedMargin??-999)-(a.pricing?.estimatedMargin??-999):s==='price'?(price(b)||0)-(price(a)||0):s==='competitors'?(b.competitors?.length||0)-(a.competitors?.length||0):title(a).localeCompare(title(b)));
 $('#library-summary').innerHTML=`<article class="metric-card"><span>Products</span><strong>${results.length}</strong></article><article class="metric-card"><span>Categories</span><strong>${cats.length}</strong></article><article class="metric-card"><span>Competitor listings</span><strong>${results.reduce((n,r)=>n+(r.competitors?.length||0),0)}</strong></article><article class="metric-card"><span>Cross-sells</span><strong>${results.reduce((n,r)=>n+(r.crossSells?.length||0),0)}</strong></article>`;
 $('#library-empty').classList.toggle('hidden',rows.length>0);
 $('#library-list').innerHTML=rows.map(r=>`<article class="card library-card"><div class="library-image">${img(r)?`<img src="${esc(img(r))}" alt="">`:'No image'}</div><div><p class="catalog-path"><strong>${esc(category(r))}</strong> › ${esc(subcategory(r))}</p><h2>${esc(title(r))}</h2><div class="meta">${r.asin?`<span>ASIN ${esc(r.asin)}</span>`:''}${r.sourceInput?.sku?`<span>SKU ${esc(r.sourceInput.sku)}</span>`:''}${r.brand?`<span>${esc(r.brand)}</span>`:''}</div><div class="intelligence-grid"><span><small>Price</small><b>${money(price(r))}</b></span><span><small>Market avg.</small><b>${money(r.pricing?.marketAverage)}</b></span><span><small>Margin</small><b>${r.pricing?.estimatedMargin!=null?esc(r.pricing.estimatedMargin)+'%':'—'}</b></span><span><small>Competitors</small><b>${r.competitors?.length||0}</b></span></div><div class="button-row"><a class="button secondary" href="/market?product=${encodeURIComponent(r.asin||title(r))}">Competitors</a><a class="button secondary" href="/pricing?product=${encodeURIComponent(r.asin||title(r))}">Pricing</a></div></div></article>`).join('');
 } ['#lib-search','#lib-category','#lib-sort'].forEach(x=>$(x).addEventListener('input',render));render();});
