
document.addEventListener('DOMContentLoaded',()=>{const {$,esc,money,load,save,title,img,category,subcategory}=PIQ;let results=load();
 function counts(fn){return results.reduce((o,r)=>(o[fn(r)]=(o[fn(r)]||0)+1,o),{})} function bars(obj){let max=Math.max(1,...Object.values(obj));return Object.entries(obj).sort((a,b)=>b[1]-a[1]).map(([k,v])=>`<div class="catalog-bar"><span>${esc(k)}</span><div><i style="width:${v/max*100}%"></i></div><strong>${v}</strong></div>`).join('')||'<p class="empty">No catalog data yet.</p>'}
 function render(){let cats=counts(category),subs=counts(subcategory);$('#catalog-metrics').innerHTML=`<article class="metric-card"><span>Products</span><strong>${results.length}</strong></article><article class="metric-card"><span>Categories</span><strong>${Object.keys(cats).length}</strong></article><article class="metric-card"><span>Cross-sells</span><strong>${results.reduce((n,r)=>n+(r.crossSells?.length||0),0)}</strong></article><article class="metric-card"><span>Upsells</span><strong>${results.reduce((n,r)=>n+(r.upsells?.length||0),0)}</strong></article>`;$('#category-list').innerHTML=bars(cats);$('#subcategory-list').innerHTML=bars(subs);
 $('#relationship-list').innerHTML=results.map(r=>`<article class="card relationship-card"><div class="relationship-product">${img(r)?`<img src="${esc(img(r))}" alt="">`:''}<div><p class="catalog-path"><strong>${esc(category(r))}</strong> › ${esc(subcategory(r))}</p><h2>${esc(title(r))}</h2></div></div><h3>Cross-sells</h3>${(r.crossSells||[]).length?`<div class="mini-products">${r.crossSells.map(x=>`<div><strong>${esc(x.title)}</strong><small>${esc(x.reason||'')} · ${money(x.price)}</small></div>`).join('')}</div>`:'<p class="muted-copy">No suitable cross-sells found in current inventory.</p>'}<h3>Upsells</h3>${(r.upsells||[]).length?`<div class="mini-products">${r.upsells.map(x=>`<div><strong>${esc(x.title)}</strong><small>${esc(x.reason||'')} · ${money(x.price)}</small></div>`).join('')}</div>`:'<p class="muted-copy">No suitable upsells found in current inventory.</p>'}</article>`).join('')||'<article class="card empty">Research products first.</article>'; }
 $('#reanalyze-catalog').onclick=async()=>{let b=$('#reanalyze-catalog');b.disabled=true;b.textContent='Analyzing…';try{let res=await fetch('/api/catalog/enrich',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({results})});let d=await res.json();if(!res.ok)throw Error(d.error||'Analysis failed');results=d.results||results;save(results);render()}catch(e){alert(e.message)}finally{b.disabled=false;b.textContent='Reanalyze full catalog'}};render();});
