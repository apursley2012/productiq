
document.addEventListener('DOMContentLoaded',()=>{const {$,esc,money,load,title}=PIQ;let results=load(),sel=$('#pricing-product');sel.innerHTML=results.length?results.map((r,i)=>`<option value="${i}">${esc(title(r))}</option>`).join(''):'<option>No saved products</option>';let wanted=new URLSearchParams(location.search).get('product');if(wanted){let i=results.findIndex(r=>(r.asin||title(r))===wanted);if(i>=0)sel.value=i;}
 function render(){let r=results[+sel.value];if(!r)return $('#pricing-summary').innerHTML='<article class="card empty">Research products first.</article>';let p=r.pricing||{},cost=$('#pricing-cost').value===''?Number(p.cost||0):Number($('#pricing-cost').value||0),fees=Number($('#pricing-fees').value||0),breakEven=cost+fees,sp=Number(p.suggestedPrice||0),profit=sp?sp-breakEven:null,margin=sp&&profit!=null?profit/sp*100:null;
 $('#pricing-summary').innerHTML=`<article class="card pricing-hero"><h2>${esc(title(r))}</h2><div class="scenario-grid"><div><span>Cost + fees</span><strong>${money(breakEven)}</strong><small>Break-even before marketplace percentage fees/tax</small></div><div><span>Market low</span><strong>${money(p.marketLow)}</strong></div><div><span>Market average</span><strong>${money(p.marketAverage)}</strong></div><div><span>Market high</span><strong>${money(p.marketHigh)}</strong></div><div><span>Competitive</span><strong>${money(p.competitivePrice)}</strong></div><div class="featured-scenario"><span>Suggested</span><strong>${money(sp)}</strong><small>${profit!=null?`${money(profit)} profit · ${margin.toFixed(1)}% margin`:''}</small></div><div><span>Premium</span><strong>${money(p.premiumPrice)}</strong></div></div></article>`;
 let cs=(r.competitors||[]).filter(c=>c.price!=null).sort((a,b)=>a.price-b.price);$('#pricing-listings').innerHTML=cs.length?`<table><thead><tr><th>Store</th><th>Price</th><th>Match</th></tr></thead><tbody>${cs.map(c=>`<tr><td>${esc(c.retailer)}</td><td>${money(c.price)}</td><td>${esc(c.matchConfidence||'')} ${c.matchScore??''}%</td></tr>`).join('')}</tbody></table>`:'<div class="empty">No priced competitor listings saved for this product.</div>'; }
 [sel,$('#pricing-cost'),$('#pricing-fees')].forEach(x=>x.addEventListener('input',render));render();});
